// Package audit persists records of remote file operations executed on the
// local workspace into a configurable SQL database (sqlite/mysql/postgres),
// so that cloud-agent activity can be reviewed after the fact.
package audit
