# Phase 6B Hardening — 测试错误

## 当前状态

- 已清零

## 2026-04-07

- 首次 `make lint` 失败：
  - `pkg/syncer/path_resolution.go` 中 `resolveActionTargetAbs` 认知复杂度超阈值
  - `pkg/syncer/handle_write_test.go` 使用了 `0755` 目录权限，被 `gosec G301` 拒绝
- 修复措施：
  - 拆分路径解析流程为 `resolveActionTargetPass` / `lstatActionPath` / `expandSymlinkTarget`
  - 将测试目录权限收紧为 `0750`
- 修复后重新执行 `make fmt`、`make lint`、`make test`、`go build ./...`：全部通过
