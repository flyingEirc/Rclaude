# Phase 5 开发流程记录

## 本次实现范围

- 扩展 `internal/inmemtest` 为多用户、可注入故障的测试 harness
- 补齐 `pkg/fusefs` 的 `inmem` 集成测试矩阵
- 新增 Linux 真 FUSE 自动化冒烟与手动脚本入口

## 命令记录

### 1. 预备

- 命令：`git config user.name "wuyuxi"` / `git config user.email "wuyuxi@9326997.xyz"`
- 结果：通过；仓库本地 git 身份已配置

### 2. 定向验证

- 命令：`go test -count=1 ./internal/inmemtest/...`
- 结果：通过（无测试文件，仅验证包可编译）

- 命令：`go test -count=1 ./pkg/fusefs/...`
- 结果：首次失败；`TestInmem_ChangeOnOneUserDoesNotInvalidateOther` 使用同长度内容，等待条件不够稳

- 命令：`go test -count=1 ./pkg/fusefs/...`
- 结果：修复用例后通过

### 3. 标准门禁

- 命令：`export PATH="$(go env GOPATH)/bin:$PATH" && make fmt`
- 结果：通过

- 命令：`export PATH="$(go env GOPATH)/bin:$PATH" && make lint`
- 结果：首次失败；`internal/inmemtest/harness.go` 和 `pkg/fusefs/mount_linux_smoke_test.go` 存在复杂度与 `gosec` 告警

- 命令：`export PATH="$(go env GOPATH)/bin:$PATH" && make fmt`
- 结果：修复 lint 后重新格式化，通过

- 命令：`export PATH="$(go env GOPATH)/bin:$PATH" && make lint`
- 结果：通过，`0 issues.`

- 命令：`export PATH="$(go env GOPATH)/bin:$PATH" && make test`
- 结果：通过，含 `-race`

- 命令：`go build ./...`
- 结果：通过

### 4. 补充验证

- 命令：`go test -count=1 -run TestMount_LinuxSmoke -v ./pkg/fusefs`
- 结果：通过；Linux 真 FUSE 冒烟在当前环境实际执行成功，未触发 `skip`

- 命令：`bash -n tools/fuse-smoke.sh`
- 结果：通过；手动脚本 shell 语法有效

## 偏离记录

- 原 spec 里提到“扩展或拆分 `pkg/fusefs/inmem_e2e_test.go`”，实际落地为保留原文件并新增 `pkg/fusefs/inmem_phase5_test.go`
- 原因是避免把 Phase 4 已稳定的用例与 Phase 5 新矩阵搅在同一个文件里，便于后续按场景维护
- 偏离只影响测试文件组织方式，不影响验收口径

## 收尾说明

- `internal/inmemtest` 已升级为多用户、可注入故障的 harness，并保留 `Start(...)` 单用户快捷入口
- `pkg/fusefs` 现有测试已覆盖多用户隔离、单边缓存失效、读超时、写超时、断线快速失败
- Linux 真 FUSE 冒烟默认纳入 `make test`，当前环境实际通过
- 手动脚本入口位于 `tools/fuse-smoke.sh`
