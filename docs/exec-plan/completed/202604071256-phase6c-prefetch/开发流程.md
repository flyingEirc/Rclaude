# Phase 6C — 开发流程

## 2026-04-07

- 创建阶段目录与三件套
- 基于 `docs/superpowers/specs/2026-04-07-phase6c-prefetch-design.md` 开始实现
- `pkg/config` 增加 `prefetch` 配置结构、默认值和加载测试
- `pkg/session/manager.go` 透传预取配置，`pkg/session/session.go` 增加 in-flight 去重
- 新增 `pkg/fusefs/prefetch.go`，在 `workspaceNode.Readdir` 成功后触发异步小文件预取
- `internal/inmemtest/harness.go` 对齐 server 默认预取配置，便于 `inmem` 路径复现真实行为
- `pkg/fusefs/view_test.go` 增加候选筛选、异步、不重复预取、离线跳过测试
- 新增 `pkg/fusefs/inmem_phase6c_test.go`，覆盖预热缓存和变更失效回归
- 执行 `go test ./pkg/config ./pkg/session ./pkg/fusefs ./internal/inmemtest -count=1`：通过
- 执行 `export PATH="$(go env GOPATH)/bin:$PATH" && make fmt`：通过
- 执行 `export PATH="$(go env GOPATH)/bin:$PATH" && make lint`：通过，`0 issues.`
- 执行 `export PATH="$(go env GOPATH)/bin:$PATH" && make test`：通过，含 `-race`
- 执行 `go build ./...`：通过
