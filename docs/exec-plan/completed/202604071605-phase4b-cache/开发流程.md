# Phase 4b 开发流程记录

## 本次实现范围

- 新增服务端整文件内容缓存
- 接入 `session` 与 `fusefs` 读路径
- 补充缓存命中、失效、禁用缓存测试

## 命令记录

### 1. 分层测试

- 命令：`go test -count=1 ./pkg/contentcache/...`
- 结果：通过

- 命令：`go test -count=1 ./pkg/session/...`
- 结果：通过

- 命令：`go test -count=1 ./pkg/fusefs/...`
- 结果：通过

- 命令：`go test -count=1 ./internal/inmemtest/...`
- 结果：通过（无测试文件，仅验证包可编译）

### 2. 格式化与静态检查

- 命令：`make fmt`
- 结果：失败，当前 Windows 环境未安装 `make`

- 命令：`gofumpt -w pkg/contentcache/cache.go pkg/contentcache/cache_test.go pkg/session/manager.go pkg/session/manager_test.go pkg/session/session.go pkg/session/session_test.go pkg/session/service.go pkg/session/service_test.go pkg/fusefs/view.go pkg/fusefs/view_test.go pkg/fusefs/inmem_e2e_test.go internal/inmemtest/inmem_transport.go app/server/main.go`
- 结果：通过

- 命令：`gci.exe write --section standard --section default --section "prefix(flyingEirc/Rclaude)" pkg/contentcache/cache.go pkg/contentcache/cache_test.go pkg/session/manager.go pkg/session/manager_test.go pkg/session/session.go pkg/session/session_test.go pkg/session/service.go pkg/session/service_test.go pkg/fusefs/view.go pkg/fusefs/view_test.go pkg/fusefs/inmem_e2e_test.go internal/inmemtest/inmem_transport.go app/server/main.go`
- 结果：通过

- 命令：`make lint`
- 结果：失败，当前 Windows 环境未安装 `make`

- 命令：`golangci-lint run ./...`
- 结果：通过，`0 issues.`

### 3. 全量验证

- 命令：`make test`
- 结果：失败，当前 Windows 环境未安装 `make`

- 命令：`go test -count=1 ./...`
- 结果：通过

- 命令：`go test -race -count=1 -timeout 180s ./...`
- 结果：失败，Windows 本机出现 `exit status 0xc0000139`

- 命令：`go build ./...`
- 结果：通过

- 命令：`$env:GOOS='linux'; $env:GOARCH='amd64'; go build ./app/server`
- 结果：通过

### 4. Linux 收尾验收

- 命令：`make tools`
- 结果：通过；安装了 `gofumpt`、`gci`、`golangci-lint` 与 proto 插件

- 命令：`export PATH="$(go env GOPATH)/bin:$PATH" && make fmt`
- 结果：通过

- 命令：`export PATH="$(go env GOPATH)/bin:$PATH" && make lint`
- 结果：首次失败；官方二进制 `golangci-lint` 由 `go1.24` 构建，无法检查 `go 1.25.2` 模块

- 命令：`go install github.com/golangci/golangci-lint/v2/cmd/golangci-lint@v2.1.6`
- 结果：通过；改为使用当前本机 Go 从源码安装同版本 `golangci-lint`

- 命令：`export PATH="$(go env GOPATH)/bin:$PATH" && make lint`
- 结果：通过，`0 issues.`

- 命令：`export PATH="$(go env GOPATH)/bin:$PATH" && make test`
- 结果：首次失败；`pkg/fusefs` 的 `TestWorkspaceNode_MkdirUnlinkRmdir` 暴露测试夹具问题，`workspaceNode` 未接入 `go-fuse` inode tree 即调用 `Create/Mkdir`

- 命令：`export PATH="$(go env GOPATH)/bin:$PATH" && make test`
- 结果：修复 `pkg/fusefs/mount_linux.go` 与 `pkg/fusefs/mount_linux_test.go` 后通过，含 `-race`

- 命令：`go build ./...`
- 结果：通过

## 偏离记录

- 按项目规范优先尝试 `make fmt` / `make lint` / `make test`，但初始实现记录来自缺少 `make` 的 PowerShell 环境
- 已按 `docs/workflow.md` 的底层命令回退执行 `gofumpt`、`gci.exe`、`golangci-lint`、`go test`、`go build`
- 阶段收尾时在 Linux 环境补做了一次完整标准门禁，并为此修复了 `pkg/fusefs` 中暴露出的既有 lint 和测试夹具问题

## 收尾说明

- Linux 环境已补做项目要求的标准门禁：`make fmt`、`make lint`、`make test`、`go build ./...`
- 收尾过程中顺带修复了 `pkg/fusefs` 中暴露出的既有 lint 和测试夹具问题，不改变 Phase 4b 的缓存目标或验收口径
