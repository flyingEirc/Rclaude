# Phase 2 测试错误记录

> 阶段目录：`docs/exec-plan/active/202604061800-phase2-daemon-mvp/`
> 每条记录格式：`Ennn | 状态 | 涉及 Todo | 症状 | 根因 | 修复`
> 状态取值：`OPEN` / `FIXED`

## 记录

- E001 | FIXED | T9 | `mingw32-make fmt` 在 Windows 环境崩溃，报 `bash.exe: couldn't create signal pipe, Win32 error 5` | 当前机器上的 MSYS `bash` 无法正常支撑 Makefile 的 shell 管道执行 | 改为直接执行 `gofumpt -l -w .` 与 `gci write ...`，完成等价格式化并继续后续验收。

## 当前状态

- 当前没有未关闭的测试/验收错误记录。
