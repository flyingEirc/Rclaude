# 202604200502-ci-format-diff

## 完成状态

done

## 验收结果

- `make fmt`：通过；最终重跑无新增文件被格式化写回。
- `make lint`：通过；最终输出 `0 issues.`。
- `make test`：通过；全量 `go test -race -count=1 -timeout 120s ./...` 通过。

## 完成内容

- 将 `app/clientpty/terminal_unix_bsd.go` 与 `app/clientpty/terminal_unix_other.go` 中相邻 ioctl 常量改为 `const (...)`，纳入 `gofumpt` 输出。
- 显式处理 client PTY terminal restore 失败路径。
- 修复 PTY 测试 harness 与 host 测试中的被忽略错误。
- 为 PTY host spawn 的 `gosec` 检查补充约束说明。
- 修复 `pkg/ptyservice` fake host wait 信息读取 race。
- 为 `pkg/syncer` daemon 测试配置补齐 PTY frame 默认值。

## 与 plan 的偏离

- 初始目标是修复 `git diff --exit-code` 暴露的格式化 diff；执行完整门禁后发现同一 CI 流程后续 `make lint` 和 `make test` 也会失败，因此扩大范围一并修复。
- 主工作区本地 `master` 落后 `origin/master` 且存在未跟踪冲突文件；为避免覆盖用户工作，本阶段在 `/tmp/Rclaude-action-format` 的 `action-format` 分支完成。

## 遗留问题

- 当前改动尚未推送到远端；需要将 `action-format` 分支上的 diff 合并或提交到触发 Action 的分支后，GitHub Actions 才会使用这些修复。
