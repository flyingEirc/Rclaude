# 开发流程

## 2026-04-20

- 从主工作区确认当前本地 `master` 落后 `origin/master` 2 个提交；Action 报错文件存在于 `origin/master`，不在当前本地 HEAD。
- 为避免覆盖主工作区未跟踪文件，创建临时 worktree `/tmp/Rclaude-action-format`，分支 `action-format`，基于 `origin/master`。
- 已查阅 `docs/design/PLAN.md`、`docs/design/ROADMAP.md` 与 `docs/workflow.md`；本阶段属于格式化修复，不变更系统设计或 Phase 范围。

## 命令记录

- `make fmt`
  - 初次输出：`gofumpt` 写回 `app/clientpty/terminal_unix_bsd.go` 与 `app/clientpty/terminal_unix_other.go`。
  - 后续输出：无新增文件被写回。
- `make lint`
  - 初次沙箱内执行因 `/root/.cache` 只读失败；按权限流程重跑。
  - 暴露并修复 `errcheck`、`gosec`、`errorlint`、`gocognit`、`govet shadow` 问题。
  - 最终结果：`0 issues.`。
- `make test`
  - 初次沙箱内执行因 `/root/.cache` 只读失败；按权限流程重跑。
  - 暴露并修复 `pkg/ptyservice` fake host race 与 `pkg/syncer` daemon 测试缺少 PTY frame 默认配置的问题。
  - 最终结果：全量 `go test -race -count=1 -timeout 120s ./...` 通过。

## 偏离说明

- 初始报错只停在 `git diff --exit-code`，但修复格式化后同一 CI 线会继续执行 `make lint` 和 `make test`；为避免下一轮 Action 继续失败，本阶段范围扩展到后续门禁失败。
- 主工作区 `/root/Rclaude` 的本地 `master` 落后 `origin/master`，且存在会与 `origin/master` 冲突的未跟踪文件；本阶段在 `/tmp/Rclaude-action-format` 临时 worktree 的 `action-format` 分支中完成。
