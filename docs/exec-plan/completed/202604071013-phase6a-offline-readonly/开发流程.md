# Phase 6A — 开发流程

## 2026-04-07

- 创建阶段目录与三件套
- 基于 `docs/superpowers/specs/2026-04-07-phase6a-offline-readonly-design.md` 开始实现
- 实现 `pkg/config` 的 `OfflineReadOnlyTTL` 配置默认值与加载测试
- 实现 `pkg/session` 的离线只读状态、TTL 过期判断与 `Manager.HandleDisconnect`
- 调整 `pkg/session/service.go` 与 `internal/inmemtest/harness.go`，统一断线收尾路径
- 调整 `pkg/fusefs/view.go`，支持离线缓存读与离线写拒绝
- 补齐 `pkg/session`、`pkg/fusefs/view_test.go`、`pkg/fusefs/inmem_phase5_test.go` 的离线只读测试
- 执行 `export PATH="$(go env GOPATH)/bin:$PATH" && make fmt`：通过
- 执行 `export PATH="$(go env GOPATH)/bin:$PATH" && make lint`：首次失败，`pkg/session/service_test.go` 出现 `shadow`；修复后再次执行通过，`0 issues.`
- 执行 `export PATH="$(go env GOPATH)/bin:$PATH" && make test`：通过，含 `-race`
- 执行 `go build ./...`：通过
