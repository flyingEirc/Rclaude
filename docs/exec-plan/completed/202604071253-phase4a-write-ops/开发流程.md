# Phase 4a 开发流程记录

## 本次实现范围

- 完成 proto 扩展：`WriteFileReq.offset`、`TruncateReq`
- 完成 daemon 写链路：`Write/Create/Mkdir/Delete/Rename/Truncate`
- 完成 `pathLocker` 与 `selfWriteFilter`
- 完成 session 本地树即时 apply
- 完成 `view` 写 helper、错误分类、请求超时封装
- 完成 Linux FUSE 写接口：`Write/Create/Mkdir/Unlink/Rmdir/Rename/Setattr(size)`
- 完成 in-memory 端到端测试链路

## 命令记录

### 1. 协议生成

- 命令：`protoc -I=api/proto --go_out=. --go_opt=module=flyingEirc/Rclaude --go-grpc_out=. --go-grpc_opt=module=flyingEirc/Rclaude api/proto/remotefs/v1/remotefs.proto`
- 结果：成功，`remotefs.pb.go` / `remotefs_grpc.pb.go` 已更新

### 2. 分层测试

- 命令：`go test -count=1 ./pkg/syncer/...`
- 结果：通过

- 命令：`go test -count=1 ./pkg/session/...`
- 结果：通过

- 命令：`go test -count=1 ./pkg/fusefs/...`
- 结果：通过

- 命令：`go test -count=1 ./pkg/config/...`
- 结果：通过

### 3. 全量验证

- 命令：`go test -count=1 ./...`
- 结果：通过

- 命令：`golangci-lint run ./...`
- 结果：`0 issues.`

- 命令：`gofumpt -w .`
- 结果：通过

- 命令：`gci.exe write --section standard --section default --section "prefix(flyingEirc/Rclaude)" .`
- 结果：通过

- 命令：`go build ./...`
- 结果：通过

- 命令：`$env:GOOS='linux'; $env:GOARCH='amd64'; go build ./app/server`
- 结果：通过

### 4. 环境限制验证

- 命令：`go test -race -count=1 -timeout 180s ./...`
- 结果：失败，Windows 本机出现 `exit status 0xc0000139`
- 说明：与仓库 `Makefile` 中的已知限制一致，Windows 本地 race runtime 不可用；非 race 全量测试、lint、普通构建、Linux 交叉构建均已通过

### 5. Linux 真机 FUSE 手动验收

- 时间：2026-04-07
- 环境：Linux 真机，挂载点 `/tmp/rclaude/mount/u1`，真实工作区 `/tmp/rclaude/workspace`

执行摘要：

- `ls -la /tmp/rclaude/mount` 显示用户目录 `u1` 已挂载
- `echo hi > "$mountpoint/foo.txt"` 成功
- `cat "$mountpoint/foo.txt"` 输出 `hi`
- `cat "$workspace/foo.txt"` 输出 `hi`
- `mkdir "$mountpoint/d"` 成功
- `ls -la "$mountpoint"` 显示 `d/` 与 `foo.txt`
- `ls -la "$workspace"` 显示 `d/` 与 `foo.txt`
- `mv "$mountpoint/foo.txt" "$mountpoint/d/bar.txt"` 成功
- `cat "$mountpoint/d/bar.txt"` 输出 `hi`
- `cat "$workspace/d/bar.txt"` 输出 `hi`
- `truncate -s 1 "$mountpoint/d/bar.txt"` 成功
- `cat "$mountpoint/d/bar.txt"` 输出 `h`
- `cat "$workspace/d/bar.txt"` 输出 `h`
- `rm "$mountpoint/d/bar.txt"` 成功
- `rmdir "$mountpoint/d"` 成功
- `find "$mountpoint" -maxdepth 2 | sort` 仅剩 `/tmp/rclaude/mount/u1`
- `find "$workspace" -maxdepth 2 | sort` 仅剩 `/tmp/rclaude/workspace`

结论：

- `Write/Create/Mkdir/Rename/Truncate/Unlink/Rmdir` 均已通过 Linux 真机验收
- 挂载点与真实工作区行为一致
- 未观察到卡死、遗漏落盘或重命名/截断异常

## 与 plan 的偏离

- 原计划 T21 使用 `internal/testutil/inmem_transport.go`
- 实际实现改为 `internal/inmemtest/inmem_transport.go`
- 原因：`internal/testutil` 若直接依赖 `pkg/syncer`，会与 `pkg/syncer` 包内测试形成 import cycle，影响全量测试
- 影响：功能不变，仍提供进程内 server/session/syncer 闭环测试能力

## 当前状态

- T1-T23：已完成代码与自动化验证
- T24：未完成归档
- 阻塞项：缺少用户侧 Linux 真机 FUSE 手动验收记录，当前目录仍保留在 `active/`
