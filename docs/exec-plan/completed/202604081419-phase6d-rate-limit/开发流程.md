# Phase 6D — 开发流程

## 2026-04-08

- 基于 `docs/superpowers/specs/2026-04-08-phase6d-rate-limit-design.md` 创建阶段目录与三件套
- 实施目标：补齐 daemon 侧读/写字节限流，默认关闭，显式配置生效
- `pkg/config` 新增 `RateLimitConfig`、daemon `rate_limit` 加载与负数校验测试
- 新增 `pkg/ratelimit/limiter.go` 与 `limiter_test.go`，实现可禁用的字节 limiter 与 context 可取消等待
- `pkg/syncer/handle.go` 改为接收 `context.Context`，`HandleOptions` 增加 `ReadLimiter` / `WriteLimiter`
- `pkg/syncer/handle.go` 在 `handleRead` 的最终 payload 上接入 `ReadLimiter`
- `pkg/syncer/handle_write.go` 在 `handleWrite` 真正落盘前接入 `WriteLimiter`
- `pkg/syncer/handle_write.go` 将 `selfWrites.Remember` 后移到限流等待成功之后，避免取消时留下伪自写抑制
- `pkg/syncer/daemon.go` 启动时从配置构造 limiter，并在 `runRecvLoop` 透传 stream 上下文到 `Handle`
- `internal/inmemtest/harness.go` 对齐 `syncer.Handle(ctx, ...)` 新签名
- `pkg/syncer/handle_test.go` / `handle_write_test.go` 增加限流生效、取消可打断、空内容 create 不限流、非字节操作不误伤回归
- `pkg/syncer/daemon_test.go` 增加读限流延迟、写限流延迟、限流等待时 daemon 取消退出回归
- 执行 `env GOCACHE=/tmp/go-build go test ./pkg/config ./pkg/ratelimit ./pkg/syncer -count=1`：通过
- 执行 `env PATH="$(go env GOPATH)/bin:$PATH" GOCACHE=/tmp/go-build make fmt`：通过；`gofumpt` 写回 `pkg/config/config.go` 与 `pkg/ratelimit/limiter.go`
- 执行 `env PATH="$(go env GOPATH)/bin:$PATH" GOCACHE=/tmp/go-build GOLANGCI_LINT_CACHE=/tmp/golangci-lint make lint`：通过，`0 issues.`
- 执行 `env PATH="$(go env GOPATH)/bin:$PATH" GOCACHE=/tmp/go-build make test`：通过，含 `-race`
- 执行 `env GOCACHE=/tmp/go-build go build ./...`：通过
- 说明：当前环境的默认 `/root/.cache` 为只读，因此验证命令统一把 Go build cache 与 golangci-lint cache 重定向到 `/tmp`
