# Phase 6B — 开发流程

## 2026-04-07

- 创建阶段目录与三件套
- 基于 `docs/superpowers/specs/2026-04-07-phase6b-sensitive-file-filter-design.md` 开始实现
- `pkg/config` 增加 `workspace.sensitive_patterns` 加载
- 新增 `pkg/syncer/sensitive_filter.go`，统一内置规则与追加规则
- `pkg/syncer/scan.go`、`watch.go`、`handle.go`、`handle_write.go` 接入敏感路径过滤
- `internal/inmemtest/harness.go` 改为按真实扫描结果 bootstrap，并复用敏感过滤器
- 补齐 `pkg/syncer` 过滤器、扫描、监听、读写语义测试
- 新增 `pkg/fusefs/inmem_phase6b_test.go`，覆盖敏感路径隐藏与写拒绝
- 执行 `go test ./pkg/syncer ./internal/inmemtest ./pkg/fusefs ./pkg/config -count=1`：首次失败，`pkg/syncer/daemon.go` 中 `serveStream` 缺少 `sensitiveFilter` 形参；修复后通过
- 执行 `export PATH="$(go env GOPATH)/bin:$PATH" && make fmt`：通过
- 执行 `export PATH="$(go env GOPATH)/bin:$PATH" && make lint`：首次失败，`pkg/syncer/handle_write.go` 复杂度超限、`pkg/syncer/watch.go` 认知复杂度超限；抽出 helper 后再次执行通过，`0 issues.`
- 执行 `export PATH="$(go env GOPATH)/bin:$PATH" && make test`：通过，含 `-race`
- 执行 `go build ./...`：通过
