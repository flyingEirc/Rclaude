# Phase 6B — 测试错误

## 当前状态

- 已清零

## 2026-04-07

- `go test ./pkg/syncer ./internal/inmemtest ./pkg/fusefs ./pkg/config -count=1` 首次失败：
  - 位置：`pkg/syncer/daemon.go`
  - 问题：`serveStream` 调用点已传入 `sensitiveFilter`，但函数签名未同步，导致 `undefined: sensitiveFilter`
  - 修复：为 `serveStream` 增加 `sensitiveFilter` 形参并向下透传
  - 结果：修复后该定向测试通过

- `make lint` 首次失败：
  - 位置：`pkg/syncer/handle_write.go`、`pkg/syncer/watch.go`
  - 问题：新增敏感过滤后，`handleWrite` 圈复杂度超限，`addTreeRecursively` 认知复杂度超限
  - 修复：抽取 `resolveMutatingPath`、`resolveRenamePaths`、`shouldSkipWatchedDir` 等 helper，压低复杂度
  - 结果：修复后 `make lint` 通过，`0 issues.`
