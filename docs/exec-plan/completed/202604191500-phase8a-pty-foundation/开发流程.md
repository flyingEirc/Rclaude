# Phase 8a — 开发流程

## 2026-04-19

- 基于 `docs/superpowers/specs/2026-04-19-remote-claudecode-pty-design.md` 创建阶段目录与三件套（plan.md / 开发流程.md / 测试错误.md）
- 实施目标：交付远端 PTY 协议（`pty.proto`）+ `pkg/ptyhost` 进程管理 + `pkg/ptyclient` 桥接 + `pkg/config` PTY 块，每个包均带单测
- 显式不在本阶段：service handler 装配、`app/clientpty` 二进制、session/ratelimit 接入、集成测试、文档与 ROADMAP 更新（留给 Phase 8b/8c）
- 先执行规划审查：review agent 结论为“计划方向可执行，但需先处理阻塞项”，主要包括脏工作树隔离、依赖版本固定、`ptyclient` 串行化 `Stream.Send`、以及跨平台测试/命令口径修正
- 为避免 `make fmt` 把主工作树的无关 `.M` 状态卷入 phase8a，创建干净 worktree：`E:\Rclaude\.codex-phase8a`，后续实现与验收均在该 worktree 中进行
- 前置依赖治理：按官方包版本固定 `github.com/creack/pty v1.1.24`、`golang.org/x/term v0.42.0`；在 clean worktree 中执行 `go get github.com/creack/pty@v1.1.24 golang.org/x/term@v0.42.0`、`go mod tidy`
- 并行执行策略：拆分为 `proto` / `config` / `ptyhost` / `ptyclient` 四个 worker，文件所有权互斥；`go.mod` / `go.sum`、最终质量门、exec-plan 归档由主代理统一收口

> 实施过程中按 plan.md 的 Task 顺序在此追加每条命令的执行结果摘要。

### 实施记录补充

- 规划审查由独立 review agent 完成，阻塞项包括：主工作树脏状态隔离、第三方依赖版本固定、`ptyclient` 不能并发 `Stream.Send`、PowerShell 下跨平台命令口径、`ResolveBinary("sh")` 测试不稳。
- 按任务拆分为 `proto` / `config` / `ptyhost` / `ptyclient` 四个执行 agent，并在 `E:\Rclaude\.codex-phase8a` clean worktree 内统一整合结果，避免污染主工作树。
- 依赖落地结果与 plan 存在一处偏离：最终保留 `github.com/creack/pty v1.1.24` 与 `golang.org/x/sys v0.43.0`，未保留 `golang.org/x/term`；`pkg/ptyclient` resize 实现改用 `x/sys/unix` / `x/sys/windows`，原因是只需窗口尺寸查询，不需要引入额外终端模式依赖。
- 本机 PowerShell 环境缺少 `make`，因此验收使用 `Makefile` 等价命令：
  - `C:\Users\stillmoke\go\bin\gofumpt.exe -l -w .`
  - `C:\Users\stillmoke\go\bin\gci.exe write --section standard --section default --section "prefix(flyingEirc/Rclaude)" .`
  - `C:\Users\stillmoke\go\bin\golangci-lint.exe run ./...`
  - `go test -count=1 -timeout 120s ./...`
- 包级验证通过：
  - `go test ./api/proto/remotefs/v1/...`
  - `go test ./pkg/config/...`
  - `go test ./pkg/ptyclient/...`
  - `go test ./pkg/ptyhost/...`
- 全仓 fresh 验证通过：
  - `golangci-lint run ./...` 输出 `0 issues.`
  - `go test -count=1 -timeout 120s ./...` 全绿
  - 三平台编译通过：`GOOS=windows/linux/darwin GOARCH=amd64 go build ./...`
- 独立 codereview 先发现两项高优先级问题：
  - `pkg/ptyclient` attach 握手阶段对 `ctx` 取消不可达
  - `pkg/ptyhost.ResolveCwd` 会放过 `alice/../bob` 这类经清洗后越权的 `userID`
- 已按 code review 修复并补回归测试：
  - `pkg/ptyclient/client_test.go` 新增“before attached 超时取消”场景
  - `pkg/ptyhost/policy_test.go` 新增归一化穿越 `userID` 场景
- code review 复审结论：`no findings for the re-review`。剩余非阻塞风险为：首帧 `Recv` goroutine 的及时回收仍依赖具体 `Stream` 实现在 `CloseSend()` 或上层 context 取消后能够解阻；当前不构成 blocker，留待 Phase 8b 联调时继续观察。
- scope 偏离已记录：为通过整仓 lint/test，保留了少量 phase 外但低风险的 mechanical changes（`internal/testutil/bufconn.go`、`pkg/contentcache/*`、`pkg/logx/logx_test.go`、`pkg/safepath/safepath.go`）以及 `pkg/syncer` 的跨平台测试兼容修正；这些改动无业务语义扩展，但需要在完成摘要中明确说明。
