# 开发流程

## 2026-04-07

### 1. 失败接入

- 用户提供 GitHub Actions `make test` 失败日志，定位到：
  - `pkg/fusefs/inmem_phase6c_test.go:42`
  - `pkg/fusefs/inmem_phase6c_test.go:64`

### 2. 上下文核对

- 已查阅：
  - `docs/design/ROADMAP.md`
  - `docs/design/PLAN.md`
  - `docs/ARCHITECTURE.md`
  - `docs/workflow.md`
  - `.github/workflows/ci.yml`

### 3. 初步结论

- 预取逻辑为异步 goroutine
- 当前测试把“请求已发出”当作“缓存已完成预热”，并发下存在时序窗口
- 计划改为等待缓存命中后再做计数断言

### 4. 代码调整

- 修改 `pkg/fusefs/inmem_phase6c_test.go`
- 将首次等待条件从 `ReadRequestCount() == 1` 调整为：
  - `Session.Lookup("prefetch.txt")` 成功
  - `Session.GetCachedContent("prefetch.txt", info)` 命中
  - 缓存内容为 `hello`
- 在收到 `MODIFY` 变更并等待文件树更新后，增加一次“新签名下缓存已失效”的断言

### 5. 验证结果摘要

- 定向并发复现：
  - `env GOCACHE=/tmp/go-build go test -race ./pkg/fusefs -run TestInmem_PrefetchWarmsCacheAndChangeInvalidates -count=20`
  - 结果：`ok  	flyingEirc/Rclaude/pkg/fusefs	1.460s`
- 格式化：
  - `env GOCACHE=/tmp/go-build make fmt`
  - 结果：通过
- 静态检查：
  - `env GOCACHE=/tmp/go-build make lint`
  - 结果：`0 issues`
  - 备注：由于当前沙箱下 `/root/.cache/golangci-lint` 只读，出现 cache 持久化 warning，但不影响 lint 结果
- 全量测试：
  - `env GOCACHE=/tmp/go-build make test`
  - 结果：通过，`pkg/fusefs` 通过，整体退出码为 `0`
- 构建检查：
  - `env GOCACHE=/tmp/go-build make build`
  - 结果：通过
