# Phase 8c — 开发流程

## 2026-04-19

- 创建 `phase8c` 阶段目录三件套，作为 `phase8b` 后续收口阶段。
- 初步拆分：
  - Worker D 负责 `app/clientpty`
  - Worker E 负责 smoke / deploy
  - Worker F 负责文档
- Worker E/F 先读取了 `api/proto/remotefs/v1/pty.proto`、`pkg/config/config.go`、`pkg/ptyclient/*.go`，确认当前真实字段为：
  - client frame: `attach` / `stdin` / `resize` / `detach`
  - server frame: `attached` / `stdout` / `exited` / `error`
  - error kind: `KIND_*`
  - server 配置: `pty.binary`、`pty.workspace_root`、`pty.env_passthrough`、`pty.frame_max_bytes`、`pty.graceful_shutdown_timeout`、`pty.ratelimit.attach_qps|attach_burst|stdin_bps|stdin_burst`
- Worker E 新增 `tools/pty-smoke.sh`，拆成两种模式：
  - `manual`：直接跑 `rclaude-claude --config <daemon.yaml>`，给真实 `claude` 入口做人肉验收
  - `scripted`：要求 server 端临时把 `pty.binary` 指向 `/bin/sh`，用 `script(1)` 驱动一段确定性输入，验证 attach、TTY 存在、stdin/stdout 和退出码透传
- Worker E 扩展 `deploy/minimal/`：
  - 新增 `start-pty.sh`
  - 在 `server.example.yaml` 补齐 `pty:` 默认字段
  - 在 `daemon.example.yaml` 明确“daemon YAML 同时给 `rclaude-daemon` / `rclaude-claude` 复用”
  - 在 `deploy/minimal/README.md` 增加 PTY 人工验收与 scripted smoke 说明
- Worker E 更新根目录 `daemon-localtest.yaml`，明确它是开发者本地联调配置，不属于 `deploy/minimal/` 交付物。
- Worker F 补充文档：
  - 新增 `docs/reference/pty-protocol.md`
  - 更新 `docs/reference/README.md`
  - 在 `docs/ARCHITECTURE.md` 追加 Phase 8 的“文件面 / 交互面”分层约束
  - 在 `docs/design/ROADMAP.md` 只补 Phase 8 的阶段边界，不写执行细节
- 根据边界校正结论再收窄了一轮：
  - `deploy/minimal/` 只做原地扩展，不引出第二套交付目录
  - `daemon-localtest.yaml` 保留在仓库根目录，专供开发者本地联调
  - `docs/reference/pty-protocol.md` 回收成“协议速查”，不混入架构/部署说明
  - `docs/design/ROADMAP.md` 只保留 Phase 8 阶段边界，不写文件级执行细节
- 验证记录：
  - `go test ./api/proto/remotefs/v1 ./pkg/config`：通过；为绕开系统缓存权限，临时把 `GOCACHE` / `GOTMPDIR` 指到 `C:\Users\stillmoke\.codex\memories`
  - `go build -o C:\Users\stillmoke\.codex\memories\clientpty.exe ./app/clientpty`：通过，说明 `app/clientpty` 当前可编译
  - `go build ./app/clientpty`：在当前 Windows 环境失败；默认输出会尝试写仓库根目录 `clientpty.exe`，返回 `Access is denied`
  - `sh -n tools/pty-smoke.sh ...`：在当前 Windows + Git Bash 环境失败，`msys-2.0.dll` 报 `couldn't create signal pipe, Win32 error 5`
  - `make lint` / `make test`：当前环境无 `make` 命令，无法执行
