# Phase 8c — 测试错误

记录要求：

- 记录脚本执行失败、示例配置错误、CLI 编译/联调失败
- 若最终无失败，收尾时明确标注“本阶段无测试失败记录”

## 2026-04-19

- `sh -n tools/pty-smoke.sh deploy/minimal/start-pty.sh deploy/minimal/start-server.sh deploy/minimal/start-daemon.sh deploy/minimal/smoke-remote.sh`
  - 失败原因：当前 Windows 环境下 Git Bash / MSYS 报 `couldn't create signal pipe, Win32 error 5`
  - 影响：本轮无法在本机完成 shell 语法检查
- `go build ./app/clientpty`
  - 失败原因：默认输出尝试写仓库根目录 `clientpty.exe`，返回 `Access is denied`
  - 结论：不是编译错误；改用 `go build -o C:\Users\stillmoke\.codex\memories\clientpty.exe ./app/clientpty` 后通过
- `make lint` / `make test`
  - 失败原因：当前环境没有 `make` 命令
  - 影响：无法在本机执行项目约定的 Makefile 验证入口
- `go test ./api/proto/remotefs/v1 ./pkg/config`
  - 结果：通过
