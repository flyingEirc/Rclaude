# Phase 8b — 开发流程

## 2026-04-19

- 基于 `docs/superpowers/specs/2026-04-19-remote-claudecode-pty-design.md` 与 `phase8a` 完成摘要，创建 `phase8b` 阶段目录三件套。
- 状态确认：
  - 文档层面 `phase8a=done`
  - 当前分支层面 `phase8a` 产物尚未提交，需视作本地未提交基线继续推进
- 并行策略：
  - Worker A 负责 `pkg/session` / `pkg/ratelimit`
  - Worker B 负责 PTY service handler 与 `app/server` 装配
  - Worker C 负责 `internal/inmemtest` / `internal/testutil` / PTY 集成测试
  - 独立 review agent 在 A/B/C 完成后执行 code review
- 注意事项：
  - 不回退用户现有改动
  - 严控提交边界，避免把无关 CRLF/格式化噪音带入本阶段
- 当前主代理已先行完成一版可注册的 `RemotePTY.Attach` 中间层实现，暂放在 `pkg/ptyservice/`，用于把协议校验、PTY 生命周期与流桥接从 `app/server` 装配层剥离。
- 已完成的本地验证：
  - `go test ./pkg/ptyservice/...` 通过
- 待整合项：
  - 等 Worker A 回传 `pkg/session` / `pkg/ratelimit` 的最小 API 后，再决定该 service 是保留为独立包，还是回收到 `pkg/session/pty_service.go`
  - `app/server` 仅在 manager/ratelimit 接口稳定后接线，避免与并行 worker 冲突
- Worker A 回传后，已在主工作树完成整合：
  - `pkg/session.Manager` 新增 live daemon lookup 与 PTY 注册/反注册
  - `pkg/ratelimit` 新增通用 limiter 与 PTY attach/stdin helper
  - `app/server` 新增 PTY registry/limiter 适配层，并将 `RemotePTY` 注册到同一个 gRPC server
- 主代理新增了 PTY gRPC 级集成测试：
  - 新增 `internal/testutil/pty_bufconn.go`
  - 新增 `pkg/ptyservice/grpc_integration_test.go`
  - 覆盖场景：正常 attach、daemon offline、session busy
- 最新定向验证：
  - `go test -count=1 ./internal/testutil ./pkg/ptyservice/... ./app/server/... ./pkg/session/... ./pkg/ratelimit/...` 通过
