# Phase 7 — 开发流程

## 2026-04-08

- 基于 `docs/superpowers/specs/2026-04-08-phase7-minimal-deploy-smoke-design.md` 创建阶段目录与三件套
- 实施目标：补齐双机最小部署模板、启动脚本、冒烟脚本和 README
- 已先对齐现有 CLI 入口：`rclaude-server --config <path>`、`rclaude-daemon --config <path>`
- 已确认 `pkg/config` 中 server/daemon YAML 字段与设计一致
- 已复用现有 `tools/fuse-smoke.sh` 风格，收敛新脚本的最小验证动作
- 新增 `deploy/minimal/README.md`、`server.example.yaml`、`daemon.example.yaml`
- 新增 `deploy/minimal/start-server.sh`、`start-daemon.sh`、`smoke-remote.sh`
- 由于挂载工作区不保留脚本执行位，README 中统一改为 `sh ./deploy/minimal/...` 调用方式
- `smoke-remote.sh` 在 `rename` 后增加短重试，避免 FUSE 目录视图短暂延迟导致误判失败
- 执行 `sh -n /workspace/localdemo/deploy/minimal/start-server.sh`：通过
- 执行 `sh -n /workspace/localdemo/deploy/minimal/start-daemon.sh`：通过
- 执行 `sh -n /workspace/localdemo/deploy/minimal/smoke-remote.sh`：通过
- 执行 `RCLAUDE_SERVER_BIN=/bin/echo sh /workspace/localdemo/deploy/minimal/start-server.sh /workspace/localdemo/deploy/minimal/server.example.yaml`：通过
- 执行 `RCLAUDE_DAEMON_BIN=/bin/echo sh /workspace/localdemo/deploy/minimal/start-daemon.sh /workspace/localdemo/deploy/minimal/daemon.example.yaml`：通过
- 执行 `sh /workspace/localdemo/deploy/minimal/smoke-remote.sh localdemo README.md`：通过，完成 `ls` / `cat` / `write` / `mv` / `rm`
- 执行 `env PATH="$(go env GOPATH)/bin:$PATH" GOCACHE=/tmp/go-build make fmt`：通过
- `make fmt` 在挂载仓库里顺带格式化了大量与本阶段无关的 Go 文件，随后已从 `/root/Rclaude` 回拷原文件，保证 workspace 中只保留 Phase 7 相关改动
- 执行 `env PATH="$(go env GOPATH)/bin:$PATH" GOCACHE=/tmp/go-build GOLANGCI_LINT_CACHE=/tmp/golangci-lint make lint`：失败，`golangci-lint` 在 FUSE 挂载中无法稳定获取 `cwd`
- 直接执行 `golangci-lint run --config /workspace/localdemo/.golangci.yml ./...`：失败，`go/packages` 返回 `getwd: no such file or directory`
- 执行 `env GOCACHE=/tmp/go-build go -C /workspace/localdemo test -race -count=1 -timeout 120s flyingEirc/Rclaude/...`：通过
- 当前结论：部署产物与真实 smoke 已闭环，仓库测试可通过模块导入路径变通执行，`lint` 仍受 FUSE 工作目录限制阻塞
- 说明：当前环境的默认 `/root/.cache` 为只读，因此所有 Go 相关验证都把 cache 重定向到 `/tmp`
