# FUSE `cwd` / 模块发现兼容性修复 — 开发流程

## 2026-04-08

- 任务来源：在 `202604081636-phase7-minimal-deploy-smoke` 的 workspace 挂载验证中发现
- 已确认 `/workspace/localdemo` 中 `ls`、`cat`、`write`、`mv`、`rm` 可用
- 已确认依赖 `cwd` 与 `./...` 包发现的 Go 工具链存在兼容性问题
- 决定将该问题单独拆成 active 阶段跟踪，避免和当前最小部署闭环任务混淆
- 当前阶段只建档，不进入实现
