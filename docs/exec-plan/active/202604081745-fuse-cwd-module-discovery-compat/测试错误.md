# FUSE `cwd` / 模块发现兼容性修复 — 测试错误

## 2026-04-08

- 在 `/workspace/localdemo` 中执行 `make lint`：
  - 现象：`golangci-lint` 报 `can't get config directory`
  - 底层错误：`getwd: no such file or directory`

- 在 `/workspace/localdemo` 中执行 `go test ./...`：
  - 现象：`pattern ./...: getwd: no such file or directory`

- 在 `/workspace/localdemo` 中执行 `go list ./...`：
  - 现象：同样依赖当前工作目录解析，无法稳定工作

- 对照现象：
  - `sh /workspace/localdemo/deploy/minimal/smoke-remote.sh localdemo README.md` 可以通过
  - 说明问题集中在目录身份 / `cwd` / 模块发现语义，而不是普通路径读写
